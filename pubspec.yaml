name: crypto_currency_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.0.2
  firebase_auth: ^1.0.1
  cloud_firestore: ^1.0.5
  google_fonts: ^2.0.0
  provider: ^5.0.0
  google_mobile_ads: ^0.12.1+1
  flutter_launcher_icons: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^1.1.7+1

flutter_icons:
  android: true
  ios: true
  image_path: "assets/cryptocurrency.png"

flutter_native_splash:
  color: "#251955"
  image: "assets/cryptocurrency.png"

flutter:
  uses-material-design: true
   
  assets:
    - assets/cryptocurrency.png